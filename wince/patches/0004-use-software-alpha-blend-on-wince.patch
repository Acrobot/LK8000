diff --git a/Common/Source/Screen/LKSurface.cpp b/Common/Source/Screen/LKSurface.cpp
--- a/Common/Source/Screen/LKSurface.cpp
+++ b/Common/Source/Screen/LKSurface.cpp
@@ -539,9 +539,22 @@
 #ifdef USE_GDI
+#ifdef UNDER_CE
+    // Windows CE 4.x does not guarantee the desktop AlphaBlend API. Keep
+    // using LK8000's software implementation which was part of the original
+    // Pocket PC target.
+    extern BOOL DoAlphaBlend_internal(HDC,int,int,int,int,HDC,int,int,int,int,DWORD);
+    return DoAlphaBlend_internal(
+            *this, dstRect.left, dstRect.top,
+            dstRect.right - dstRect.left, dstRect.bottom - dstRect.top,
+            Surface, srcRect.left, srcRect.top,
+            srcRect.right - srcRect.left, srcRect.bottom - srcRect.top,
+            globalOpacity);
+#else
     //BLENDFUNCTION bf = { AC_SRC_OVER, 0, globalOpacity, AC_SRC_ALPHA };
     // we are not using per-pixel alpha, so do not use AC_SRC_ALPHA flag
     BLENDFUNCTION bf = {AC_SRC_OVER, 0, globalOpacity, 0};
     ::AlphaBlend(*this, dstRect.left, dstRect.top, dstRect.right - dstRect.left, dstRect.bottom - dstRect.top,
             Surface, srcRect.left, srcRect.top, srcRect.right - srcRect.left, srcRect.bottom - srcRect.top, bf);
 
     return true; // always return true because always implemented on Windows PC
+#endif
 #else
